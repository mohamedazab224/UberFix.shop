# 🧪 دليل اختبار دورة حياة طلب الصيانة

## 📋 نظرة عامة

تم إنشاء نظام اختبار شامل لدورة حياة طلب الصيانة الكاملة يختبر جميع المراحل والانتقالات.

## 🚀 كيفية الوصول

يمكنك الوصول لصفحة الاختبار عبر:
- مباشرة: `/lifecycle-testing`
- من صفحة الاختبارات: `/testing` ← اختبار دورة حياة طلب الصيانة

## ✅ ما يتم اختباره

### 1. تحضير البيئة
- التحقق من تسجيل الدخول
- التحقق من وجود company_id للمستخدم
- إنشاء شركة اختبارية إذا لزم الأمر
- إنشاء فرع اختباري

### 2. إنشاء طلب صيانة اختباري
```
- العنوان: "طلب صيانة اختبار - دورة حياة كاملة"
- الوصف: "اختبار شامل لجميع مراحل دورة الحياة"
- الحالة الأولية: DRAFT
- الأولوية: high
```

### 3. اختبار جميع الانتقالات (Workflow Transitions)
الاختبار يمر عبر جميع المراحل بالترتيب:

1. **DRAFT → SUBMITTED** (تقديم الطلب)
2. **SUBMITTED → TRIAGED** (فرز الطلب)
3. **TRIAGED → ASSIGNED** (تخصيص فني)
4. **ASSIGNED → SCHEDULED** (جدولة الموعد)
5. **SCHEDULED → IN_PROGRESS** (بدء التنفيذ)
6. **IN_PROGRESS → INSPECTION** (الفحص)
7. **INSPECTION → COMPLETED** (اكتمال العمل)
8. **COMPLETED → BILLED** (إصدار الفاتورة)
9. **BILLED → PAID** (الدفع)
10. **PAID → CLOSED** (إغلاق الطلب)

### 4. اختبار الحالات الجانبية
- **ON_HOLD** (تعليق الطلب)
- **WAITING_PARTS** (بانتظار القطع)
- **CANCELLED** (إلغاء الطلب)

### 5. التحقق من سجل الأحداث
- فحص جدول `request_events`
- التأكد من تسجيل جميع التغييرات

### 6. التنظيف النهائي
- حذف الطلب الاختباري
- حذف الفرع الاختباري
- عرض ملخص النتائج

## 📊 النتائج

سيعرض الاختبار نتائج تفصيلية لكل خطوة:
- ✅ **نجاح**: عملية تمت بنجاح
- ❌ **فشل**: حدث خطأ أثناء العملية
- ⚠️ **تحذير**: العملية تمت مع ملاحظات

### ملخص النتائج النهائي
```
نجح: X | فشل: Y | تحذيرات: Z
```

## 🔍 تفاصيل إضافية

كل نتيجة تحتوي على:
- **الخطوة**: اسم المرحلة المختبرة
- **الحالة**: success/error/warning
- **الرسالة**: وصف النتيجة
- **التفاصيل**: (اختياري) معلومات تقنية إضافية

## ⚙️ المتطلبات

- يجب أن يكون المستخدم مسجل دخول
- صلاحيات كافية لإنشاء وتعديل طلبات الصيانة
- الاتصال بقاعدة البيانات

## 🛡️ الأمان

- الاختبار لا يؤثر على البيانات الفعلية
- جميع البيانات الاختبارية يتم حذفها تلقائياً
- إذا فشل الحذف، يظهر تحذير

## 📝 ملاحظات

1. **الاختبار الآلي**: يتم تنفيذ جميع الخطوات تلقائياً
2. **الوقت المتوقع**: 10-15 ثانية للاختبار الكامل
3. **التزامن**: ينتظر 300ms بين كل انتقال
4. **الأخطاء**: في حالة فشل خطوة، يستمر الاختبار للخطوات التالية

## 🔧 استكشاف الأخطاء

### المشكلة: "يجب تسجيل الدخول أولاً"
**الحل**: قم بتسجيل الدخول قبل بدء الاختبار

### المشكلة: "فشل الانتقال من X إلى Y"
**السبب المحتمل**: 
- قيود على الانتقالات في قاعدة البيانات
- مشكلة في دالة `can_transition_stage`
- صلاحيات RLS غير كافية

**الحل**: تحقق من:
1. سياسات RLS لجدول `maintenance_requests`
2. دالة `can_transition_stage` في قاعدة البيانات
3. صلاحيات المستخدم الحالي

### المشكلة: "تعذر جلب الأحداث"
**السبب المحتمل**: مشكلة في trigger أو سياسات RLS لجدول `request_events`

**الحل**: 
1. تحقق من وجود trigger `log_request_stage_change`
2. راجع سياسات RLS لجدول `request_events`

## 📈 التحسينات المستقبلية

- [ ] إضافة اختبار للإشعارات
- [ ] اختبار تخصيص الفنيين تلقائياً
- [ ] اختبار SLA timers
- [ ] اختبار المرفقات والصور
- [ ] اختبار الموافقات (approvals)

## 🎯 الخلاصة

هذا الاختبار الشامل يساعدك على:
1. ✅ التأكد من عمل جميع مراحل دورة الحياة
2. ✅ اكتشاف أي مشاكل في الانتقالات
3. ✅ التحقق من سياسات RLS
4. ✅ فحص تسجيل الأحداث
5. ✅ ضمان سلامة البيانات
